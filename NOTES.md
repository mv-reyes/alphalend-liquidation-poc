# open threads

- verify how zero-threshold markets are created (isolated vs non-native)
- devInspect end-to-end needs a funded liquidator wallet, out of scope here
- seize path reads bonus/fee from the SEIZED market config, not the debt market
